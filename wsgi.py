from bot import web_app

if __name__ == "__main__":
    web_app.run()
```    *   **Apa artinya ini?** File ini hanya bertugas satu hal: mengimpor objek `web_app` (aplikasi Flask Anda) dari file `bottle.py`.